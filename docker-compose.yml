version: '3.8'

services:
  skaptix-website:
    image: skaptix-website:latest
    container_name: skaptix-website
    restart: unless-stopped
    ports:
      - "5000:80"
    env_file:
      - .env.prod
